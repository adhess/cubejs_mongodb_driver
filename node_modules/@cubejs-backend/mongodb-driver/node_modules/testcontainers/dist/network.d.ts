import { CreateNetworkOptions } from "./docker-client";
import { DockerodeClientFactory } from "./docker-client-factory";
import { Uuid } from "./uuid";
export declare class Network {
    private readonly uuid;
    private readonly dockerClientFactory;
    private readonly createNetworkOptions;
    constructor(createNetworkOptions?: Partial<CreateNetworkOptions>, uuid?: Uuid, dockerClientFactory?: DockerodeClientFactory);
    start(): Promise<StartedNetwork>;
}
export declare class StartedNetwork {
    private readonly id;
    private readonly options;
    private readonly dockerClientFactory;
    constructor(id: string, options: CreateNetworkOptions, dockerClientFactory: DockerodeClientFactory);
    getId(): string;
    getName(): string;
    stop(): Promise<void>;
}
